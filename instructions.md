Good now that we have that formatted file listed in the filestore, when user clicks on it, the ArtifactViewer panel (src/lib/components/workspace/ArtifactViewer.svelte) will open to show the content, we should also add a floating pill/chip (maybe top right) to indicate the state/status (for a failed state we should show an error button with a badge that count the errors, when user clicks on it we should open the validation viewer to see the errors).

Note that there is 2s auto--save trigger that happens, we should also parse the document (src/lib/utils/marksheet-ast-parser.ts) then do a zod schema validation (src/lib/schema/marksheet.ts), this now going to be our error feedback. 