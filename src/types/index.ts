/**
 * ==========================================
 * Layer: TYPES
 * ==========================================
 * Purpose:
 *   Shared Typescript types, exact Enums, and generic bounds used 
 *   across the entire application.
 * 
 * Rules:
 *   - No executable code allowed (only `type` and `enum`).
 *   - Database-specific inferred types (e.g., `typeof schema...`) should 
 *     ideally live in the `db/` or `repositories/` layer unless globally required.
 */

// export * from './auth.types';
