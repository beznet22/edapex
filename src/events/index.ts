/**
 * ==========================================
 * Layer: SYSTEM EVENTS (EDA)
 * Protocol: @backend-architect
 * ==========================================
 * Purpose:
 *   Defines domain events decoupled from the actual workflow. 
 *   For example, standardizing the payload shapes broadcast when 
 *   an entity implies a change of state elsewhere in the system.
 */

// export * from './UserRegisteredEvent.ts';
