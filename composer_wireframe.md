    
```
    (Parent Rounded Corner)                                         (Parent Rounded Corner)                                    
     .------------------------------------------------------------------------------------. 
    /  Access [M↓] ~/Workspace/smscore/docs/architecture.md?     [ Deny ] [ Validate ^]    \ ---------> ActionBar 
   | .------------------------------------------------------------------------------------. |
   |/ (Input Top Rounded Corner)                                 (Input Top Rounded Corner)\|
   | |Ask anything, @ to mention, / for workflows |                                         |
   |                                                                                        |  -------->ChatComposer
   |                                                                                        |
   | + mic-icon |                                                                    [■]    |
    \                                                                                      /
     '------------------------------------------------------------------------------------' 
   (Shared Bottom/Input Corner)                                (Shared Bottom/Input Corner)

```


**Reference behavior** (from user-provided descriptions):

```
                                                             
   ┌──────────────────────────────────────────────────────────┐  
   │  ┌──────────┐                                            │  
   │  │Large     │  ============   (loading skeleton bars)    │  
   │  │File Icon │  ============                              │  
   │  │Tilted 10°│  "You'll be informed immediately..."       │  
   │  └──────────┘ (changing info - filename, timestamp, etc) │  
   │                                                          |
   │  [====skeleton====]                 (eye preview icon)   │ 
   └──────────────────────────────────────────────────────────┘  

   ┌──────────────────────────────────────────────────────────┐  
   │  ┌──────────┐                                            │  
   │  │ Document │   Artifacts                  (done title)  │  
   │  │  mock    │   filename.md                              │  
   │  └──────────┘                                            │  
   │                                                          │  
   │  22/05/2026 10:25                   (eye preview icon)   │  
   └──────────────────────────────────────────────────────────┘ 
```

### Finishing touches for the Edapex AI Platform

1. Redesign the Thinking test to be dynamic, slick and modern, should have a shimmer effect when thinking, then static when done the change to "Thought for N sec", use chevron rigt and chevron down icon instead. add your own optimization modern industry recommendations, we also achitect a structure thinking text in which the thinking test are seperated by sub header/topic/title.

2. audit the variants selection lifecycle / user journey, and ensure consistency with the LLM providerOption and thinkingEffort. thinkingEffort is decided by provoder based this logic: `In thinking mode, for compatibility, low and medium are mapped to high, and xhigh is mapped to max`

- **Thinking vriant mapping**
   ```typescript
   // for thinking mode only, provider like deepseek
   export const thinkingEffortSchema = z.enum(['auto', 'fast', 'thinking']);

   export const thinkingEffortMap = {
   thinking: { "thinking": "enabled", effort: "high" }, // thinking enabled
   fast: { "thinking": "disabled", effort: "low" }, // thinking disabled, low effort
   auto: { "thinking": "disabled", effort: "low" } // by default, not thinking, effort is based prompt complexity and length, we can use a model to calculate the effort. or use the context length
   }

   // for thinking mode only - example from deepseek
   {
   model="deepseek-v4-pro",
   # ...
   reasoning_effort="high",
   extra_body={"thinking": {"type": "enabled"}}
   }

   ```
- **Thinking vriant mapping**
   Also we need variant selection dropdown to be jus a text with chevron down not a button or use a text only button

3. We also need to optimize the assistantAgent (src/lib/server/mastra/agents/assistant.ts) tool calling capability/inteliengence, to calling should be proactive and know what to do without too much thinking;

4. Right now the documentAgent does not know he is inside an document/artifact editor. also the document agent should accept a title for the document/artifact generated by the assistantAgent, so that we can save the artifact with a title and file name later when the document tool returns its tool output.

5. Based No. 4, the assistantAgent should also be able to decide whether to create a new document/artifact or update an existing document/artifact. It should also decide the filename and title of the document/artifact by checking the exist file under its workspace directory, so we can have a consistant document naming convention and file management system. 

6. The transcript processing lifecycle. should the same flow as the marksheet processing except that the transcript processing begins by first retrieving a transcript data already available on the database/db instead of uploading a file.

7. We need to ensure that transcript/marksheet publishing tool are properly and can generate pdf, push to student timeline (marksheet only) and send email to parents and the assistant agent should proactively decide to use these tools when applicable.

8. audit other assistant agent tools to ensure all tenant context context will be properly injected on run time. tha major failure point of all tools is the missing or failed to retreive tenant context completely resulting to fauled queries to db. Assistant agent should inteligently do HITL when context is incomplete, missing or insufficient context, instead of failing. And the agent should be able to retrieve the missing context, do HITL aproval then try again. (the flow is: Agent detects missing context -> Agent calls get_context tool to retrieve missing context -> Agent validates the retrieved context -> If context is still insufficient, Agent initiates HITL via the approval tool to get user approval -> After approval, Agent proceeds with the tool call). 

9. Based No. 8, we need architect how HITL (human-in-the-loop) should work when the assistant agent needs user approval to proceed with a tool call, especialy for the assistant agent tools. The current implementation is not user friendly and not intuitive, it uses workflow suspend/resume (this too over engineering). we should have an aproval tool that has the requireApprovl flag such that anytime the agent hits a guardrail or needs user approval to proceed with a tool call, it should use this tool, which trigers the ActionBar UI to show approval options (accept/reject or contextual user-action. eg. ask to use) the tool will ouput the next step/action based on user's response to the action bar. This is more intuitive and user friendly. (the flow is: user message -> assistant agent -> agent detects it needs user aproval -> agent calls approval tool -> tool sets requireApprovl = true -> assistant agent will pause and output a message indicating HITL -> action bar detects requireApprovl = true -> action bar shows approval options -> user clicks approve/reject -> action bar sends response to assistant agent -> agent wakes up and continues based on user response -> agent outputs next action or final response. eg. asking question, use tool, )

10. The workspace panel header does not currently show the icon buttons and the three vertical dots menu, we need to bring it back, add feature to to download the currently opened document/artifact as markdown file or pdf file (if document type is a marksheets or transcript, we should generate student result pdf on server side and download the pdf file instead of markdown file). Also we need to add a pdf viewer in the workspace panel to view the pdf file. if the artifact is a markshhet or transcript, we should add am icon button in the workspace panel to view the pdf file. the pdf viewer should be able to view the pdf file in the workspace panel.




**Task 1 — Dynamic Thinking UI**
- Redesign the Thinking test to be dynamic, slick and modern, should have a shimmer effect when thinking, then no shimmer when done. the shimer should be only on the thinking text. change to "Thought for N mins N secs" only should appear after the thinking is done. add your own optimization modern industry recommendations,
- the BrainIcon/statuse aware icon is currently not showing up, we need to make it visible. please debug
- the thinking text should be muted/grayed out slightly (`text-muted-foreground`)
- we already have a shimmer component in src/lib/components/ai-elements/shimmer
- Thinking should be collaped by default, should be be expanded only when the user clicks on the "Thinking" text or the brain icon/status aware icon.
- use tailwind for any new styles or components, ensure consistency with the existing styles and components

**Task 2 — Variant Selection + Thinking Effort Mapping **
-  Variant selection is not for deepseek model only should be for all openai-compatible providers. current all BUILTIN providers are openai-compatible. So UI should be only visible for providers and models that support thinking effort (for the providers that do not support thinking effort, the UI will be hidden automatically, because they will drop the thinking effort field automatically).

**Task 3 — Optimize Assistant Agent Tool Calling **
- The assistant agent should produce structured reasoning with Thinking sections, thinking sections should start with `### Thinking:` and end with `### End of Thinking`. Each section should have a title and a description.
- if a file is mentioned in the user prompt, the assistant agent should try to retrieve the file from the mastra workspace and read its content, then use the content to answer the user's prompt. For example for marksheets and transcripts, the agent should try to retrieve exiting marksheets and transcript files in the workspace and read their content, then use the content to answer the user's prompt via the streamDocument tool, assistant should never output document/artifact contents inline.

**Task 5 — Assistant Create-or-Update Document Decision Flow**
- mastra workspace provides all necessary tools for document/artifact creation, management, update, delete and retrieval. So the assistant agent should be able to decide whether to create a new document/artifact or update an existing document/artifact based on the user prompt. Please investigate the mastra workspace docs

**Task 7 — Publishing Tools Verification + Proactive Triggers**
 - generate-result-pdf.ts → fetches marksheet, renders ResultTemplate, writes PDF, returns preview URL.          
 - publish-result-pdf.ts → generates PDF if needed, sends email, writes timeline. (assistant should trigger HITL with action summary to confirm before using this tool)                                                                                           
 - generate-transcript-pdf.ts → same as generate-result-pdf.ts but for transcripts.                             
 - publish-transcript-pdf.ts → same as publish-result-pdf.ts but for transcripts. (assistant should trigger HITL with action summary to confirm before using this tool)

**Task 8 — Tenant Context Audit + HITL Fallback**
- assistant agent calls the `getContextTool` to retrieve tenant context if the injected context is insufficient. Then agent should trigger HITL with action summary of retrieved/injected context to confirm before using the tool, after approval the agent can proceed to use the tool. (note: injected context are IDs only this is not sufficient readable presentation to user, the retrieved context on the other hand, presents key information which the agent can use to create a readable format that can be presented to the user for HITL approval)
- audit getContextTool and ensure it is working as expected, it should return full tenant context information (for example {className: "X", sectionName: "A", classId: "1", sectionId: "1", etc}), then assistant agent should use the context to create a readable format that can be presented to the user for HITL approval
- the HITL (human-in-the-loop) approval interface should be intuitive and user-friendly, it should display the action summary in a clear and concise manner, and it should provide options for the user to approve or reject the action

**Task 9 — Approval-Tool HITL Architecture**
- The New approval tool should have a requireApproval flag (see https://mastra.ai/blog/tool-approval to understand how it works)

**Task 10 — Workspace Panel Header + PDF Viewer**
- Debug the stream end event, currently the switching to editor view is not working as expected, because it should only happen when the stream end event is received and the document/artifact agent is done with its response. (The stream end event is not received when the assistant agent is done with its response) actions icons and three dots menu are not visible after the response is fully received. Move actions outside {#if !isStreaming} and disable them instead

**Task 11 — @file Mention Popover UI + Logic** (Additional task added today)
- Currently the @file mention popover in the ChatComposer is not working as expected, because it should display all files in the workspace when the user types @file, random UUID generatd file name that can user can't relate to any document/artifact is displayed in the popover. Investigate and fix. 


Do a proper invesigation of all of the above mentioned tasks and ensure that they are all working as expected. Also check for any other issues that may have been introduced and fix them. Then do a final plan and share with me


Refinements: 
 ### Task 1 — Dynamic Thinking UI 
 - We dont need to Parse the reasoning text for ### Thinking: ... ### End of Thinking sections. Remove this from the plan. keep the thinking content as is. and implement the thinking/status aware icon.

 ### Task 3 — Assistant structured reasoning + file retrieval 
 - The UI will not re-render the thinking content. The thinking content should be a pure markdown structured content and will be displayed in the reasoning block as it is.

 ### Task 5 — Create-or-update document decision 
 - please investigate how mastra workspace API works and implement the document/artifact creation, update, delete and retrieval logic in the assistant agent. (check https://mastra.ai/docs/workspace/filesystem)
 - also investigate and ensure that mastra workspace provides all the necessary tools for document/artifact creation, update, delete and retrieval.
 
 ### Task 8 — getContextTool full context + HITL summary  
 - `getContextTool` should not be called immediately by the assistant agent at the beginning. Instead the tenant context should be injected into the assistant agent system prompt, and the assistant agent should use the context to create a readable format that can be presented to the user for HITL approval. `getContextTool` should be used by the assistant agent only when it needs additional context that is not provided in the system prompt.
 
 ### Task 9 — Approval tool architecture 
 - i just realised we dont need the approval tool architecture at all, instead of approval tool, we should have a requireApproval flag for permission based tools. (Please investigate all tools first and update only those that require approval)
 - i just realised assistant agent should only call the validate-marksheet/transcript tools to validate/approve/skip a document/artifact and `validate-marksheet/transcript`, `publish-result-pdf/publish-transcript-pdf` tools should have the requireApproval flag set to true (for HITL approval/rejection/validation/skip actions before using these tools). So `validate-marksheet/transcript` tools should be called after the `streamDocument` tool is done streaming.
 - the HITL (human-in-the-loop) approval interface should be intuitive and user-friendly, it should display the action summary in a clear and concise manner, and it should provide options for the user to approve or reject the action

 ### Task 11 — @file mention UUID filenames   
- All @mentions (@<studentName> to show scoped students dropdown, @file to show documents/artifacts dropdown, @year to show scoped academic year dropdown, @term/@exam to show examTypes dropdown) are not showing the appropriate dropdown list. Investigate and fix.

More Refinement:
### Task 3 — Assistant Structured Reasoning + File Retrieval (Simplified) 
-  Assistant emits ### Thinking: sections as pure markdown in its reasoning output. The UI renders them      
 as-is via <Reasoning> component as seen in src/lib/components/chat.svelte.

  ### Task 5 — Create-or-Update Document Decision (Mastra Native Tools)
  - We dont need the new manage-document.ts (manageDocumentTool) we already have it in the mastra workspace, the workspace has all the necessary tools for document/artifact creation, update, delete and retrieval.
  - No need for any Assistant instructions addition, it already knows how to use the workspace tools.

  ### Task 9 — requireApproval Flag on Permission-Based Tools 
  - How should action summary be generated?
  - How should action summary be presented to the user for HITL approval?
  - all tools that has requireApproval flag set to true should also have a reason field to provide the reason for the action, this is the same as the action summary
  - the action summary should be generated by the assistant agent and should be presented to the user for HITL approval.

  
  More Refinement:
  ### Task 3 — Assistant Structured Reasoning + File Retrieval 
  -  Assistant emits ### Thinking: sections as pure markdown in its reasoning output. The UI renders them      
 ```ts                                                                                                           
   instructions.push(                                                                                            
     '',                                                                                                         
     '### STRUCTURED THINKING ###',                                                                              
     'When reasoning through a problem, structure your thinking as markdown sections. Keep title short.', 
     'The thinking block should start with `### Thinking: <main_task_title>` and end with `### End of Thinking`.',                            
     'Each thinking section should start with `<title>` and end with `### End of Thinking`.',      
     'Inside each section, write a clear description of your analysis',                                                                            
     '',                                                                                                         
     '### FILE RETRIEVAL ###',                                                                                   
     'If the user mentions a file, use the workspace tools to read it before responding.',                       
     'NEVER output document/artifact contents inline in your text response.',                                    
     '',                                                                                                         
     '### TOOL CALLING DISCIPLINE ###',                                                                          
     '1. NEVER inline marksheet/transcript/report content in your text response.',                               
     '2. Call `streamDocument` for marksheets and `transcript-report` for transcripts.',                         
     '3. Call tools on the first turn when intent is clear.'                                                     
   );                                                                                                            
 ``` 

  ### Task 8 — Tenant Context in System Prompt
  - TENANT CONTEXT should not just have a single student but all the students in the class/section in a token efficient way
  - Must ensure all TenantLike field are all injected including all the students details (Names, IDs, admission numbers) in the class/section

```ts
   interface TenantLike {
   schoolId?: number | string;
   userId?: number | string;
   staffId?: number | string;
   designationId?: number | string;
   roleId?: number | string;
   classId?: number | string;
   sectionId?: number | string;
   examId?: number | string;
   examTypeId?: number | string;
   academicId?: number | string;
   studentId?: number | string;
   }
 ```




### Action Bar: HITL approval Area

* **Status Indicator:** On the left, there is a shield-shaped icon indicating a security or status message.
* **Main Message:** The action summary message reads "**Marksheet validation required**".
* **Context:** Below the main message, it specifies "**marksheets/12 · 2nd term**".
* **Actions:**
* On the right, there is a "**Skip**" button with a slash-circle icon.
* Next to it is a prominent orange "**Validate**" button, which also includes a checkmark icon and a dropdown arrow.



### ChatComposer: Input Area

* **Prompt:** A placeholder text reads "**Ask anything, / for commands @ for context...**".
* **Tools:** On the left, there is a plus sign (+) for adding items and a microphone icon for voice input.
* **Status & Submission:**
* On the right, the text "Thinking" is displayed with a small dropdown arrow, indicating the system is currently processing or waiting for input.
* A grayed-out "upload/send" arrow icon is positioned at the far right corner, suggesting it is currently inactive.